<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mqtt.list">
      <field name="name">mqtt list</field>
      <field name="model">mqtt.mqtt</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mqtt.action_window">
      <field name="name">mqtt window</field>
      <field name="res_model">mqtt.mqtt</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mqtt.action_server">
      <field name="name">mqtt server</field>
      <field name="model_id" ref="model_mqtt_mqtt"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mqtt" id="mqtt.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mqtt.menu_1" parent="mqtt.menu_root"/>
    <menuitem name="Menu 2" id="mqtt.menu_2" parent="mqtt.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mqtt.menu_1_list" parent="mqtt.menu_1"
              action="mqtt.action_window"/>
    <menuitem name="Server to list" id="mqtt" parent="mqtt.menu_2"
              action="mqtt.action_server"/>
    -->
  </data>
</odoo>